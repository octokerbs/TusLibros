import Box from "@mui/material/Box";
import { Metadata } from "next";

export const metadata: Metadata = {
    title: "TusLibros - Checkout",
};

export default function Home() {
    return (
        <Box
            sx={{ bgcolor: "#F3FCF0", width: "100vw", overflow: "auto" }}
        ></Box>
    );
}
